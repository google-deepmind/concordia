# Extra requirements for the negotiation evaluation framework.
# Install with: pip install -r requirements.in
#
# After installing concordia (pip install -e .), run:
#   pip install -r requirements.in
#   pip install transformers==4.44.0 accelerate==0.33.0  # Version fix

# Evaluation framework - statistical analysis
scikit-learn
scipy
statsmodels

# Interpretability framework - activation capture
torch
transformer-lens
accelerate

# Sparse Autoencoders (Gemma Scope)
sae-lens>=0.5.0

# HuggingFace model access
huggingface_hub

# Note: After installing, fix transformer-lens compatibility:
#   pip install transformers==4.44.0 accelerate==0.33.0
